# MuShop Environment Configuration

# ============================================================================
# ELASTICSEARCH MANAGED OPENTELEMETRY (MOTLP) - REQUIRED
# ============================================================================

# Elasticsearch Managed OpenTelemetry endpoint
# Format: https://<deployment-id>.apm.<region>.cloud.es.io
# Example: https://abc123def456.apm.us-central1.gcp.cloud.es.io
ELASTIC_OTLP_ENDPOINT=

# Elasticsearch API Key or Secret Token for authentication
# You can create this in Kibana: Stack Management → API Keys
# Or use APM Server secret token from deployment settings
ELASTIC_OTLP_TOKEN=

# Deployment environment label (production, staging, development, etc.)
DEPLOYMENT_ENVIRONMENT=production

# ============================================================================
# APPLICATION CONFIGURATION (Optional overrides)
# ============================================================================

# PostgreSQL Configuration
# POSTGRES_USER=mushop
# POSTGRES_PASSWORD=mushop

# MinIO Configuration
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin

# Kafka Configuration
# KAFKA_ADVERTISED_HOST_NAME=kafka
# KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181

# SMTP Configuration (for newsletter service)
# SMTP_HOST=mailhog
# SMTP_PORT=1025
# SMTP_USER=test
# SMTP_PASSWORD=test

# ============================================================================
# DEVELOPMENT MODE (Optional)
# ============================================================================

# Enable local Jaeger UI for development
# Set COMPOSE_PROFILES=dev to start Jaeger alongside Elasticsearch export
# COMPOSE_PROFILES=dev
